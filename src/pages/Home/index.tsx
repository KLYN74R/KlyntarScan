import React from 'react';
import { HomeLayout } from '../../layouts';

const Home: React.FC = () => {
  return (
    <HomeLayout>
        To get started, select a workflow...
    </HomeLayout>
  );
};

export default Home;